<Window x:Class="StockManagementSystem.NnMessage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StockManagementSystem"
        mc:Ignorable="d"
        Title="NnMessage" Height="127" Width="320" ShowInTaskbar="False" AllowsTransparency="True" WindowStyle="None" Background="#00000000">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="1"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.RowSpan="3" Grid.ColumnSpan="2" CornerRadius="12" Background="White"/>
        <Border Grid.Row="1" Grid.ColumnSpan="2" Background="#FF212121"/>
        <Button Grid.ColumnSpan="2" Style="{DynamicResource ButtonStyleClose}" Width="24" Height="24" Padding="4" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="2" Click="btclose_click">
            <ContentControl Template="{StaticResource bt_close}"/>
        </Button>
    </Grid>
</Window>
